# Handless Chess

for running on localhost:

To initiate the frontend, in the project directory run ```npm start```

To initiate the backend, in the src directory run ```node express.js```

To initiate pyaudio and flask backend, in the src directory run ```python3 app.py```
